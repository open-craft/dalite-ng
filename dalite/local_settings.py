# Local settings for your deployment.  You can make git ignore changes in this file using
# git update-index --assume-unchanged local_settings.py

# You must generate a secret key.  One way to do this is this command:
# python -c 'import random; print "".join(random.SystemRandom().choice("abcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*(-_=+)") for i in range(50))'

# SECURITY WARNING: keep the secret key used in production secret!
# SECRET_KEY = <add secret key here>
